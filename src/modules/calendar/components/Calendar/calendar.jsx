import * as React from 'react';
import classNames from 'classnames';

import './calendar.css';


function Calendar() {
  return (
    <section className={classNames('calendar')}>Calendar</section>
  );
}

export default Calendar;
